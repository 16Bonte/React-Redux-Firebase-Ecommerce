let initState = {
    products: [],
    prodNumber: 0,
    total: []
}

let cartReducer = (state = initState, action) => {
    switch (action.type) {
        default: 
            return state
    }
}

export default cartReducer
